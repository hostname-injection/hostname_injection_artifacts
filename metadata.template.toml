# Draft IEEE S&P artifact metadata.
#
# This follows the fields emitted by https://github.com/jelenamirkovic/artmeta.
# Before HotCRP submission, replace every example.org URL with the final
# anonymous artifact URL or permanent DOI-backed URL, as appropriate.

badge = "r"
artifact_url = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL"
cd = "b"
citation = """
@inproceedings{hostname-code-ccd-2027,
  title = {When Hostnames Become Code: Detecting Persisted Hostname Injection at Production Scale},
  booktitle = {Proceedings of the 48th IEEE Symposium on Security and Privacy},
  year = {2027}
}
"""
license_url = """
https://polyformproject.org/licenses/noncommercial/1.0.0/
https://creativecommons.org/licenses/by-nc/4.0/
"""

infrastructure_constraints = "No special hardware is required for the kick-the-tires path or full test suite. The artifact runs on commodity CPU Linux/macOS hosts with Python 3.11 and conda. Full HIB-Real replay requires staging the separate full de-identified release bundle and enough local SSD storage for the bundle, extracted JSONL, audits, and temporary files."
infrastructure_access = "Evaluators can run the artifact on their own machine or public research infrastructure using the README and ARTIFACT_EVALUATION.md commands. No SSH service, GUI, paid API, or production-network access is required."
install_script = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/install_conda.sh"
use = "The artifact supports offline evaluation of CCD detector mechanics, CAHO checkpoint use, HIB public-bundle validation, de-identification gates, and replay metric recomputation. The checked-in 150-row HIB sample proves mechanics, privacy gates, and fixed-FPR replay on reviewed public scores and release-safe public calibration groups; paper-scale headline metrics require the separate full HIB-Real release bundle."
destructive = "Evaluator commands write temporary files and explicit output paths only. They do not modify private source data or contact production services. Treat hostname strings as untrusted input and do not paste them into shells, SQL consoles, template renderers, or network-enabled callback harnesses."

claim1 = "CCD scoring and split-conformal thresholding are executable from the public code."
script1 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/ARTIFACT_EVALUATION.md#paper-claim-map"
expected1 = "The focused tests for scoring, cone partitioning, model pipeline, calibration, and calibrated-threshold bundle persistence pass."

claim2 = "The artifact can be kicked-the-tires without private data."
script2 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/run_artifact_smoke.py"
expected2 = "The smoke command compiles code, validates the public bundle in-repo and after extraction, recomputes sample replay metrics, trains a tiny CCD model, calibrates a fixed-FPR threshold into a self-contained model bundle, refreshes P_B plus global/grouped thresholds from a clean benign window, scores, explains, and certifies from that refreshed bundle with grouped thresholds, evaluates the CAHO checkpoint, and prints artifact smoke status: pass."

claim3 = "The checked-in public HIB sample bundle satisfies release gates and does not disclose raw-hostname recurrence facts."
script3 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/deidentification_release/scripts/validate_release_gate.py"
expected3 = "The validator exits successfully with status=pass and row_count_checked=150."

claim4 = "Public replay metrics can be recomputed from released rows."
script4 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/deidentification_release/scripts/recompute_metrics.py"
expected4 = "The output includes label_accounting, calibration, fixed_fpr_replay, resolved_row_replay, and detector_overlap blocks. The checked-in sample reports grouped public calibration, fixed_fpr_replay.status=available, and nonzero TP, FP, TN, and FN accounting."

claim5 = "Paper-scale headline metrics require the separate full HIB-Real de-identified release."
script5 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/ARTIFACT_EVALUATION.md#full-hib-replay-path"
expected5 = "After the full bundle is staged, the same validators and recompute_metrics.py command validate the 200,339,886-row replay and produce the fixed-FPR metrics described in the paper."

claim6 = "A clean DOI-ready archive can be assembled from the worktree."
script6 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/build_artifact_archive.py"
expected6 = "The archive builder verifies required manifest paths, excludes local caches and virtualenvs, summarizes archive contents, and writes a tarball with SHA-256 and file-manifest sidecars."

claim7 = "Static source-reachability accounting for the paper's 50-repo scope check is release-safe and recomputable."
script7 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_source_reachability_metrics.py"
expected7 = "The output reports the paper's CodeQL and Semgrep true-positive, false-positive, and missed-delayed-path counts plus derived precision/recall accounting. Full end-to-end reruns require staging the public corpus manifest and labeled tool outputs."

claim8 = "Public-scope taxonomy accounting is release-safe and separates public anchors from HIB positives."
script8 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_public_scope_metrics.py"
expected8 = "The output reports 118/127 post-freeze public reports mapped to target categories, allowed exclusion reasons, and public anchors marked as not counted in HIB training or production positives. Full end-to-end reruns require staging individual public-report label exports."

claim9 = "The artifact exposes a local latency smoke benchmark for the encoder and CCD scoring kernel."
script9 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/benchmark_artifact_latency.py"
expected9 = "The command reports hardware-dependent encoder and ccd_scores_logpriors_topk timing fields. It exercises the artifact paths but does not assert the paper's production latency table on evaluator hardware."

claim10 = "Live-overlap aggregate accounting is release-safe and recomputable."
script10 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_live_overlap_metrics.py"
expected10 = "The output reconstructs Table 7 overlap cells, derives CCD-only verified live positives, reviewed-item PPV, per-day review load, and marks controlled case-study traces as non-compromise claims. Full end-to-end reruns require staging masked live-overlap labels."

claim11 = "Aggregate paper metric tables for baseline audit, ablations, hostile mimicry, and synthetic-real validity are release-safe and recomputable."
script11 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_paper_metric_tables.py"
expected11 = "The output validates Tables 5, 6, 10, and 12 plus Appendix F aggregate metrics and derives summary checks. Full end-to-end reruns require staging full replay, ablation, LLM-baseline identifier provenance, and hostile-mimicry outputs."

claim12 = "HIB dataset-profile and verified-positive profile aggregate accounting is release-safe and recomputable."
script12 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_hib_profile_metrics.py"
expected12 = "The output validates Table 3 source/resolved/unresolved rows, Appendix B label and capture-policy counts, Table 4/Table 9 verified-positive family and obfuscation denominators, and derives replay prevalence and profile consistency checks. Row-level reproduction requires the full HIB-Real release."

claim13 = "Decision-stability, family-holdout/depth, drift-refresh, and public-real scope aggregates are release-safe and recomputable."
script13 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_stability_scope_metrics.py"
expected13 = "The output validates Figure 7 finite-edit certificate coverage and scope limits, Section 8.2 holdout/depth ranges, Section 8.3 benign-only drift-refresh invariants, independent replay tolerance, and public-real recall ordering. End-to-end reruns require the full HIB-Real release and staged stress outputs."

claim14 = "Production latency and throughput aggregates are release-safe and recomputable."
script14 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_production_latency_metrics.py"
expected14 = "The output validates Figure 5 full-path latency and throughput aggregates, scoring-kernel latency ordering, Table 5 CCD p99/p99.9 alignment, and the boundary that local evaluator hardware is not expected to reproduce production latency."

claim15 = "Controlled sink-evidence traces for audited metadata-to-code paths are release-safe and recomputable."
script15 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_sink_evidence_metrics.py"
expected15 = "The output validates the three Table 8 controlled matching-sink replay cases, requires parser/persistence/consumer/effect/detector-boundary fields, verifies CCD fired before downstream consumption, and records the traces as evidence rather than production-compromise claims."

claim16 = "Evaluation units and reproducibility boundaries are release-safe and recomputable."
script16 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_evaluation_accounting.py"
expected16 = "The output validates Table 2 denominator units, validates Appendix E/Table 11 released-versus-withheld boundaries, confirms unresolved rows are excluded from resolved replay denominators, and checks that Table 11 external boundaries are represented in manifest external completion items."

claim17 = "Table 1 and Appendix C method contracts are executable and release-safe."
script17 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_method_contracts.py"
expected17 = "The output validates Table 1 contract rows, checks CCD default configuration against Appendix C, verifies E1-E12 edit-manifest coverage and deterministic finite-edit closure, confirms CAHO two-view contrastive training plus an L2-normalized binary head with explicit AdamW weight decay, and confirms model bundles can persist config, axes, priors, and calibrated thresholds."

claim18 = "Named paper claims, figures, tables, equations, and appendices are mapped to artifact evidence and external boundaries."
script18 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_paper_claim_coverage.py"
expected18 = "The output validates 33 coverage items covering five contributions, Eq. 1, Proposition 5.1, Lemma C.1, Figures 1-7, Tables 1-12, and Appendices A-F; checks artifact path and manifest claim alignment; and requires full-data or production-private dependencies to remain represented as external completion items."

claim19 = "Finite-edit decision-stability helpers implement deterministic manifest closure."
script19 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/tests/test_certify.py"
expected19 = "The focused certification tests exercise margin-bound certificates, deterministic edit-ball enumeration, randomized smoothing boundaries, calibrated margin checks, and rejection on decision flips."

claim20 = "CAHO benchmark training supports the deployed contrastive plus binary-auxiliary training path."
script20 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/tests/test_benchmark_training.py"
expected20 = "The focused training tests exercise BenchmarkBinaryContrastiveTrainer fit/save behavior with a two-view dataset, contrastive loss, L2-normalized binary classifier head, explicit AdamW weight decay, and checkpoint report persistence."

claim21 = "The paper's headline numeric claims are centrally audited against release-safe aggregate artifacts."
script21 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/recompute_paper_headline_claims.py"
expected21 = "The output validates nine abstract/contribution/conclusion headline anchors, including replay scale, CCD fixed-FPR recall, latency, live-overlap added value, label totals, decision-stability coverage, source/public scope, and hostile-mimicry ranges."

claim22 = "All release-safe paper-claim checks can be run from one evaluator command."
script22 = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/scripts/run_artifact_claim_checks.py"
expected22 = "The output reports status=pass and runs the readiness audit, aggregate recomputation scripts, headline-claim audit, and public sample fixed-FPR replay metrics without private data."

hw = """
Kick-the-tires path: 4 CPU cores, 8 GB RAM, repository plus 5 GB free disk.
Full HIB-Real replay: CPU/IO-bound workload; provision local SSD space according to the final full release bundle size and extracted JSONL size.
GPU is not required.
"""
sw = """
Python 3.11, conda, numpy, scipy, PyTorch, sentence-transformers, idna, pytest, sentencepiece, scikit-learn.
Use environment.yml or scripts/install_conda.sh.
"""
api = "No API keys or paid online services are required."
gui = "n"

provenance = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/ARTIFACT_PROVENANCE_AND_ETHICS.md"
ethics = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/ARTIFACT_PROVENANCE_AND_ETHICS.md"
readme = "https://example.org/REPLACE_WITH_FINAL_ARTIFACT_URL/blob/main/README.md"
